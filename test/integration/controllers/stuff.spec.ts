import Stuff from '@controllers/stuff';

describe('Stuff controller - Sample tests', () => {

  beforeAll(done => {

    done();
  });

  afterAll(done => {
    done();
  });

  beforeEach(done => {
    done();
  });

  afterEach(done => {
    done();
  });

  it('Should return a sample stuff', done => {
    // TODO: implement
    done();
  });
});